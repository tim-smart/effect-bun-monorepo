{
  "name": "@app/http",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "description": "",
  "scripts": {
    "build": "bun build ./src/main.ts --target bun --outdir ./dist",
    "docker": "pnpm build && docker build ."
  },
  "dependencies": {
    "@effect/platform-bun": "^0.3.1",
    "effect": "^2.0.0-next.31"
  }
}
